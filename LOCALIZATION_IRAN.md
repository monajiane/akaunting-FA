# بومی‌سازی Akaunting برای ایران (fa-IR)

این پچ، نسخه‌ی رسمی Akaunting را برای استفادهٔ کاربران ایرانی آماده می‌کند.
تغییرات اعمال شده:

## ۱. زبان و تقویم
- زبان پیش‌فرض به **فارسی (fa-IR)** تغییر یافته (`APP_LOCALE=fa-IR`).
- منطقه زمانی به `Asia/Tehran` تنظیم شده است.
- جهت قلم به صورت خودکار **RTL** می‌شود (قبلاً در `config/language.php` موجود بود).
- کلاس کمکی `App\Utilities\Persian` اضافه شد که شامل:
  - `Persian::digits($str)` – تبدیل اعداد لاتین/عربی به فارسی
  - `Persian::toJalali($gy,$gm,$gd)` – تبدیل میلادی به شمسی (الگوریتم Borkowski)
  - `Persian::toGregorian($jy,$jm,$jd)` – تبدیل شمسی به میلادی
  - `Persian::jdate('Y/m/d')` – فرمت تاریخ شمسی با نام ماه‌های فارسی
- هلپرهای سراسری: `pnum()`، `jdate()`، `to_jalali()`
- دایرکتیو بِلید: `@jdate($timestamp)` و `@jdate('Y/m/d', $timestamp)`
- میدل‌ور `LocalizeForIran`:
  - اعداد لاتین/عربی در متن‌های HTML را به فارسی تبدیل می‌کند
  - از تغییر اعداد درون `<script>`، `<style>`, `<code>`، `value=""`, `href/src/content` جلوگیری می‌کند
  - هدر `Content-Language: fa-IR` را اضافه می‌کند
  - تگ `<html lang="fa-IR" dir="rtl">` و استایل‌شیت `public/css/iran.css` را تزریق می‌کند

## ۲. واحد پول
- واحد پیش‌فرض به **تومان ایران (IRT)** با نماد «تومان» تغییر کرد:
  - دقت: ۰ (بدون اعشار، مناسب تومان)
  - جداکننده هزارتایی: `،` (علامت فارسی)
  - ممیز: `٫`
- ریال (IRR) هم به‌روزرسانی شد:
  - بدون اعشار، نماد «ریال»، جداکننده‌های فارسی
- نرخ تبدیل ریال به تومان: **0.10** (هر ۱۰ ریال = ۱ تومان)
- هم در `config/money.php` و هم در `public/money.json` سمت کلاینت ثبت شد.

## ۳. مالیات
- در هنگام نصب با زبان فارسی، یک نرخ مالیات به نام
  **مالیات بر ارزش افزوده ۹٪** به‌صورت خودکار ایجاد می‌شود.

## ۴. سیدرهای پایگاه‌داده
- `database/seeds/Currencies.php` به جای USD، دو واحد IRT و IRR می‌سازد.
- `database/seeds/Accounts.php` حساب صندوق را با واحد IRT می‌سازد.
- `database/seeds/Settings.php`
  - روش‌های پرداخت آفلاین ایرانی را اضافه می‌کند:
    **نقدی، حواله بانکی/شبا، کارتخوان (POS)، کارت به کارت، چک**
  - پیش‌فرض‌های زیر را برای fa-IR تنظیم می‌کند:
    - کشور: IR، واحد پول: IRT، زبان: fa-IR
    - شروع سال مالی: ۰۱-۰۱ (۱ فروردین)
    - فرمت تاریخ: `Y/m/d` (مثلاً ۱۴۰۳/۰۶/۲۳)

## ۵. استایل و فونت
- فایل `public/css/iran.css` اضافه شد:
  - پشته‌قلم فارسی: `Vazirmatn, IRANSans, Tahoma, Noto Sans Arabic, Segoe UI`
  - راست‌چین شدن فرم‌ها و inputها
  - جهت درست فیلدهای عددی/ایمیل/URL (LTR درون متن RTL)
  - نمایش صحیح مبالغ (tabular-nums + LTR isolation)

## ۶. نحوه نصب
```bash
# بعد از ساخت .env و تنظیمات دیتابیس:
php artisan install --locale=fa-IR --company-name="شرکت من" \
    --company-email="info@company.ir" --admin-email="admin@company.ir" \
    --admin-password="******" --db-host=localhost --db-name=akaunting \
    --db-username=root --db-password=secret --no-interaction

# یا در وب نصب (wizard)، زبان فارسی را انتخاب کنید – مقادیر پیش‌فرض ایران اعمال می‌شود.
```

> برای اینکه اعداد و تاریخ در کل رابط به فارسی/شمسی نمایش داده شوند،
> در تمپلیت‌های دلخواه می‌توانید از `@jdate($document->issued_at)` و
> برای تبدیل دستی اعداد از `{{ pnum($number) }}` استفاده کنید.

## ۷. یادداشت
- Akaunting از کتابخانه‌ی `akaunting/money` استفاده می‌کند؛ واحد تومان
  چون یک واحد غیر ISO است با کد ۰ تعریف شده و فقط برای استفاده داخلی
  شماست. برای ارسال به درگاه‌های بین‌المللی لازم است نرخ تبدیل در بخش
  Settings → Currencies تنظیم شود.
- تقویم شمسی فقط در سمت نمایش اعمال شده. در صورت نیاز به شمسی‌سازی
  کامل فیلدهای `date` در دیتابیس (مثلاً در فاکتورها) می‌توان از پکیج
  `morilog/jalali` یا `hekmatinasser/verta` در Jobهای مربوط به CreateDocument
  استفاده کرد (به صورت پچ ثانویه).
